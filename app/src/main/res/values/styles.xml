<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/iconColorRipple</item>
        <item name="toolbarStyle">@style/toolbarStyle</item>
    </style>

    <style name="toolbarStyle">
        <item name="titleTextColor">@color/iconColorUnchecked</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="FirebaseAuthUI" parent="FirebaseUI">
        <item name="titleTextColor">@color/iconColorUnchecked</item>
        <item name="android:textColor">@color/iconColorUnchecked</item>
    </style>

    <style name="selectableItemBackground">
        <item name="colorControlHighlight">@color/iconColorRipple</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.ActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/iconColorRipple</item>
        <item name="toolbarStyle">@style/toolbarStyle</item>
    </style>


</resources>
